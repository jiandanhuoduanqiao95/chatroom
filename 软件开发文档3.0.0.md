# 聊天室系统 —— 软件开发文档 v3.1.0

> 编写日期：2026-06-14 | 版本：3.1.0

---

## 目录

1. [系统概述](#1-系统概述)
2. [架构设计](#2-架构设计)
3. [通信协议规范](#3-通信协议规范)
4. [数据库设计](#4-数据库设计)
5. [模块详细说明](#5-模块详细说明)
6. [消息处理流程](#6-消息处理流程)
7. [安全设计](#7-安全设计)
8. [测试体系](#8-测试体系)
9. [部署与运行](#9-部署与运行)
10. [已知问题与改进方向](#10-已知问题与改进方向)
11. [客户端技术栈演进路线](#11-客户端技术栈演进路线)

---

## 1. 系统概述

### 1.1 项目定位

一个基于 TCP 协议的客户端-服务器(C/S)即时通讯系统，支持 SSL 加密通信、好友管理、群组聊天、文件传输等功能。

### 1.2 技术指标

| 指标 | 值 |
|------|-----|
| 编程语言 | Python 3.12 |
| 传输协议 | TCP + TLS/SSL |
| 应用层协议 | 自定义二进制帧协议 |
| 数据持久化 | SQLite3 |
| 密码存储 | bcrypt 哈希 |
| 图形界面 | tkinter (ttk) |
| 并发模型 | 每客户端一线程 |
| 测试框架 | pytest 9.1.0 |
| 测试用例数 | 89 个（4 层） |

### 1.3 文件清单

```text
chatroom/
├── server/server_main.py             # 服务端入口
├── server/server_client_handler.py   # 客户端连接与认证
├── server/server_message_handler.py   # 消息路由核心
├── server/server_admin_handler.py    # 管理员命令
├── server/server_group_handler.py    # 群组管理
├── client/gui/gui_main.py            # 客户端主窗口
├── client/gui/gui_login_ui.py        # 登录/注册 UI
├── client/gui/gui_chat_ui.py         # 聊天主界面
├── client/gui/gui_message_handler.py # 客户端消息处理
├── client/gui/gui_admin_ui.py        # 管理员面板
├── client/gui/gui_group_ui.py        # 群组管理 UI
├── protocol.py                       # 通信协议编解码
├── database.py                       # 数据库操作层
├── SSL/gen_cert.py                   # SSL 证书生成
├── tests/                            # 自动化测试
│   ├── test_protocol.py              #   L0 协议层 (15 tests)
│   ├── test_database.py              #   L1 数据层 (35 tests)
│   ├── test_client_logic.py          #   L2 客户端逻辑 (19 tests)
│   ├── test_server.py                #   L3 服务端集成 (16 tests)
│   └── test_e2e.py                   #   L4 端到端 (4 tests)
├── run_tests.sh                      # 一键测试脚本
├── pyproject.toml                    # 项目配置
└── README.md                         # 项目说明
```

---

## 2. 架构设计

### 2.1 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                      Client (tkinter)                    │
│  ┌──────────┐ ┌──────────┐ ┌───────────┐ ┌──────────┐ │
│  │ LoginUI  │ │ ChatUI   │ │ AdminUI   │ │ GroupUI  │ │
│  └────┬─────┘ └────┬─────┘ └─────┬─────┘ └────┬─────┘ │
│       └──────────────┴─────────────┴─────────────┘      │
│                          │                               │
│               MessageHandler (消息收发)                   │
│                          │                               │
│                   protocol.py (编解码)                    │
└──────────────────────────┬──────────────────────────────┘
                           │  TCP + SSL
┌──────────────────────────┴──────────────────────────────┐
│                      Server                              │
│                   protocol.py (编解码)                    │
│                          │                               │
│              ClientHandler (认证 & 连接管理)              │
│                          │                               │
│  ┌───────────────┐ ┌──────────────┐ ┌───────────────┐  │
│  │MessageHandler │ │ AdminHandler │ │ GroupHandler  │  │
│  │  消息路由      │ │  管理命令     │ │  群组管理      │  │
│  └───────┬───────┘ └──────┬───────┘ └───────┬───────┘  │
│          └─────────────────┴─────────────────┘          │
│                          │                               │
│                    database.py (SQLite)                  │
└─────────────────────────────────────────────────────────┘
```

### 2.2 服务端架构

- **并发模型**: 每客户端一个 `daemon` 线程，由 `handle_client()` 处理完整生命周期
- **认证阶段**: `login`/`register` → bcrypt 验证 → 加载离线数据 → 进入消息循环
- **消息循环**: `process_messages()` 阻塞读取 → 按 `msg_type` 路由到对应 handler
- **客户端映射**: `client_map = {username: ssock}`，有 `threading.Lock` 保护

### 2.3 客户端架构

- **主窗口**: `ClientGUI` 持有全局状态（socket, username, chat_windows, message_status 等）
- **UI 组件**: 5 个类（LoginUI / ChatUI / AdminUI / GroupUI / MessageHandler），通过 `client_gui` 引用互相访问
- **消息接收**: 独立线程运行 `listen_for_messages()` → `process_message()` → `root.after()` 更新 UI

---

## 3. 通信协议规范

### 3.1 帧格式

```
偏移   长度     内容
────────────────────────────────────
 0      4 字节   消息头长度（big-endian uint32）
 4      N 字节   JSON 消息头（UTF-8 编码）
4+N     M 字节   消息体（原始字节）
────────────────────────────────────
```

### 3.2 消息头字段

```json
{
  "type": "chat",           // 消息类型（必填）
  "length": 123,            // 消息体字节数（必填）
  "to": "alice",            // 目标用户（可选）
  "from": "bob",            // 发送者（可选）
  "message_id": "uuid",     // 消息唯一 ID（可选）
  "filename": "doc.pdf",    // 文件名（file 类型）
  "filesize": "1024000",    // 文件大小（file 类型）
  "group_id": "1",          // 群组 ID（群组相关）
  "status": "sent",         // 消息状态（可选）
  "history": "true",        // 离线消息标记（服务端→客户端）
  "response_type": "xxx",   // admin_response 子类型
  "action": "xxx"           // admin_command 子命令
}
```

**注意**：所有 extra_headers 的 key 和 value 都会被强制转为字符串（防御数字类型用户名）。

### 3.3 消息类型全集

| 类型 | 方向 | 载荷格式 | 说明 |
|------|------|---------|------|
| `login` | C→S | 文本: 用户名 | 登录，密码在 extra_headers.password |
| `register` | C→S | 文本: 用户名 | 注册，密码在 extra_headers.password |
| `chat` | 双向 | 文本 | 私聊消息 |
| `file` | 双向 | 二进制 | 文件数据 |
| `file_request` | S→C | 空 | 通知有文件待接收 |
| `file_response` | C→S | 空 | 接受/拒绝文件（extra_headers.response） |
| `group_chat` | 双向 | 文本/JSON | 群聊消息 |
| `group_file_request` | S→C | 空 | 群文件待接收通知 |
| `group_file_response` | C→S | 空 | 群文件接受/拒绝 |
| `friend_request` | C→S | 空 | 发起好友请求 |
| `list_friend_requests` | C→S | 空 | 查询待处理请求 |
| `accept_friend` | C→S | 空 | 接受好友请求 |
| `reject_friend` | C→S | 空 | 拒绝好友请求 |
| `list_friends` | C→S | 空 | 刷新好友列表 |
| `create_group` | C→S | 文本: 群名 | 创建群组 |
| `join_group` | C→S | 文本: 群ID | 加入群组 |
| `list_groups` | C→S→C | JSON 数组 | 群组列表 |
| `recall` | 双向 | 空 | 撤回消息 |
| `receipt` | C→S | 空 | 消息回执 |
| `status_update` | S→C | 空 | 消息状态更新通知 |
| `admin_command` | C→S | 变体 | 管理员命令 |
| `admin_response` | S→C | JSON | 管理员命令响应 |
| `admin_auth` | S→C | 文本 | 管理员认证成功 |
| `error` | S→C | 文本 | 错误消息 |

### 3.4 分块传输

`protocol.py` 中 `send_message()` 和 `recv_message()` 均支持 `chunk_size` 参数（默认 4MB）。大文件分多次 `sendall()` 发送，接收端通过 `recvall()` 循环收齐。

---

## 4. 数据库设计

### 4.1 表结构

#### users（用户表）

| 列 | 类型 | 约束 | 说明 |
|----|------|------|------|
| id | INTEGER | PRIMARY KEY | 自增 ID |
| username | TEXT | UNIQUE NOT NULL | 用户名 |
| password_hash | TEXT | NOT NULL | bcrypt 哈希 |
| is_admin | BOOLEAN | DEFAULT FALSE | 管理员标记 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

#### offline_messages（离线消息表）

| 列 | 类型 | 约束 | 说明 |
|----|------|------|------|
| id | INTEGER | PRIMARY KEY | 自增 ID |
| message_id | TEXT | UNIQUE NOT NULL | 消息唯一 ID |
| sender | TEXT | NOT NULL | 发送者 |
| receiver | TEXT | NOT NULL | 接收者 |
| message_type | TEXT | NOT NULL | 消息类型 |
| content | BLOB | NOT NULL | 消息体 |
| filename | TEXT | | 文件名 |
| status | TEXT | DEFAULT 'sent' | sent/delivered/recalled |
| timestamp | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 时间戳 |

#### friends（好友关系表）

| 列 | 类型 | 约束 | 说明 |
|----|------|------|------|
| user1 | TEXT | NOT NULL, PK | 请求方 |
| user2 | TEXT | NOT NULL, PK | 目标方 |
| status | TEXT | DEFAULT 'pending' | pending/accepted |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

**设计要点**：好友关系为单向请求 + 双向确认。接受后写入两条 `accepted` 记录（双向），查询时用 UNION。

#### file_requests（私聊文件请求表）

| 列 | 类型 | 说明 |
|----|------|------|
| message_id | TEXT UNIQUE NOT NULL | 请求 ID |
| sender / receiver | TEXT NOT NULL | 发送方/接收方 |
| filename / filesize | TEXT / INTEGER | 文件信息 |
| content | BLOB NOT NULL | 文件数据（存库） |

#### groups（群组表）

| 列 | 类型 | 说明 |
|----|------|------|
| id | INTEGER PRIMARY KEY | 群组 ID |
| group_name | TEXT UNIQUE NOT NULL | 群组名 |
| created_by | TEXT NOT NULL | 创建者 |
| created_at | TIMESTAMP | 创建时间 |

#### group_members（群成员表）

| 列 | 类型 | 说明 |
|----|------|------|
| group_id + username | COMPOSITE PK | 群组 ID + 用户名 |
| joined_at | TIMESTAMP | 加入时间 |

#### group_file_requests（群文件请求表）

类似 `file_requests`，多了 `group_id` 字段。

#### group_file_responses（群文件响应表）

| 列 | 类型 | 说明 |
|----|------|------|
| message_id + group_id + username | COMPOSITE PK | |
| response | TEXT | 'accept' / 'reject' |

### 4.2 状态流转

```
消息状态:   sent ──→ delivered ──→ (cleanup_delivered_messages 删除)
                     │
                     └──→ recalled（撤回标记，不删除）

好友请求:  pending ──→ accepted（接受）
                     │
                     └──→ (delete) （拒绝，直接删除）
```

---

## 5. 模块详细说明

### 5.1 protocol.py —— 通信协议

**职责**: 消息的二进制序列化与反序列化。

```python
send_message(sock, msg_type, content, extra_headers=None, chunk_size=4MB)
recv_message(sock, chunk_size=4MB)
recvall(sock, n)  # 确保接收 n 字节（内部使用）
```

**关键逻辑**:
- `send_message`: 构造 JSON 头 → 发送 4 字节头长 → 发送 JSON → 分块发送消息体
- `recv_message`: 读 4 字节头长 → 读 JSON → 根据 `length` 字段分块收消息体
- `recvall`: 循环 `recv()` 直到收满 n 字节（处理 TCP 分包）
- 防御：`extra_headers` 所有键值对强制 `str()` 转换

### 5.2 database.py —— 数据访问层

**职责**: 封装所有 SQLite 操作，提供业务级 API。

**连接管理**:
- 每次操作通过 `_get_connection()` 上下文管理器获取连接
- 使用 `check_same_thread=False` 允许跨线程访问
- **注意**：每次操作开/关连接，非连接池模式

**核心方法分类**:
| 分类 | 方法 |
|------|------|
| 用户管理 | `add_user`, `get_user`, `user_exists`, `delete_user`, `get_all_users` |
| 离线消息 | `save_offline_message`, `get_offline_messages`, `cleanup_delivered_messages`, `update_message_status`, `get_message_info` |
| 好友系统 | `add_friend_request`, `accept_friend_request`, `reject_friend_request`, `get_friends`, `get_pending_friend_requests`, `is_friend`, `has_pending_request` |
| 群组系统 | `create_group`, `join_group`, `get_user_groups`, `get_group_members`, `is_group_member` |
| 文件传输 | `save_file_request`, `get_file_request`, `get_pending_file_requests`, `delete_file_request` |
| 群文件 | `save_group_file_request`, `get_group_file_request`, `get_pending_group_file_requests`, `delete_group_file_request`, `save_group_file_response`, `all_members_responded` |

### 5.3 server_main.py —— 服务端入口

```python
class Server:
    def __init__(self, host="127.0.0.1", port=8090)
    def build_listen()  # SSL socket → accept → 每客户端一个线程
```

- 监听 `127.0.0.1:8090`
- 使用 SSL/TLS（证书从 `SSL/` 目录加载）
- 每个客户端连接分配一个 daemon 线程

### 5.4 server_client_handler.py —— 客户端处理器

**流程**:
1. SSL 握手 → `recv_message` 获取 login/register
2. 验证：`bcrypt.checkpw(password, stored_hash)`
3. 登录成功 → `client_map[username] = ssock`
4. `load_offline_data()`: 推送离线消息、文件请求
5. `send_initial_data()`: 推送好友列表、群组列表
6. `process_messages()`: 进入消息处理循环
7. 断开或异常 → `client_map.pop(username)` + `close()`

### 5.5 server_message_handler.py —— 消息路由器

**职责**: 根据 `msg_type` 将消息分派到对应的处理逻辑。

**核心路由表**:

| msg_type | 处理逻辑 |
|----------|---------|
| `chat` | 检查好友关系 → 保存离线消息 → 在线则转发 |
| `file` | 区分私聊/群聊 → 保存文件请求 → 通知接收方 |
| `file_response` | 验证权限 → 接受则传输文件 → 拒绝则通知发送方 |
| `friend_request` | 检查目标存在/非好友 → 保存请求 → 在线则通知 |
| `accept_friend` / `reject_friend` | 更新状态 → 通知双方 |
| `recall` | 检查权限(发送者) + 时间(2分钟) + 状态(非已撤回) → 更新状态 → 通知对方 |
| `admin_command` | 转交 `AdminHandler.handle_admin_command()` |
| `create_group` / `join_group` / `group_chat` / `group_file_response` | 转交 `GroupHandler` |

**撤回时间检查**（4 处，已统一写法）:
```python
message_time = datetime.strptime(timestamp, '%Y-%m-%d %H:%M:%S').replace(tzinfo=UTC)
current_time = datetime.now(UTC)
time_diff = current_time - message_time
if time_diff > timedelta(minutes=2):
    # 拒绝撤回
```

### 5.6 server_admin_handler.py —— 管理员命令

| 命令 | action | 权限 | 说明 |
|------|--------|------|------|
| 列出用户 | `list_users` | admin | 返回所有用户及在线状态 |
| 删除用户 | `delete_user` | admin | 级联删除好友/文件/群组关联 |
| 发公告 | `announcement` | admin | 向所有在线用户广播 `[系统公告]` |

### 5.7 server_group_handler.py —— 群组管理

**群组创建**: `create_group` → 写入 groups 表 + group_members → 通知创建者 → 返回群 ID

**群聊广播**: `group_chat` → 验证群存在 + 用户是成员 → 为每个成员保存离线消息（格式: `{text, group_id}` JSON） → `notify_group_members()` 实时广播在线成员

**群文件**: `group_file_request` → 保存请求 → 广播给所有成员（跳过发送者） → 成员逐个 `group_file_response` → `all_members_responded()` 检查是否全部响应 → 清理请求

**已知设计缺陷**: `all_members_responded()` 要求所有成员（包括发送者）都响应，但发送者从不响应自己的请求，导致群文件请求永远不会被自动清理。

### 5.8 客户端模块

#### gui_main.py
持有全局状态的单例对象：
- `ssock`: SSL socket
- `chat_windows`: {friend_name: tk.Text}
- `chat_histories`: {friend_name: [{text, tag}, ...]}
- `message_status`: {message_id: "sent"/"delivered"}
- `message_lines`: {message_id: (friend, line_number)}  # 用于点击撤回
- `pending_file_requests`: 文件请求队列
- `processed_group_file_requests`: 已处理群文件请求集合（去重）

#### gui_message_handler.py
**核心方法**:

| 方法 | 功能 |
|------|------|
| `listen_for_messages()` | 阻塞循环接收消息（独立线程）|
| `process_message(header, data)` | 按 msg_type 分发处理|
| `update_message_status_in_chat()` | 更新 UI 中的消息状态 |
| `send_chat()` | 发送私聊/群聊消息 |
| `send_file()` | 选择文件并发送|
| `process_file_queue()` | 顺序弹出文件请求对话框 |
| `on_chat_text_click()` | 点击消息触发撤回 |
| `logout()` | 清理所有状态并断开连接 |

---

## 6. 消息处理流程

### 6.1 私聊消息完整流程

```
Client A                    Server                     Client B
   │                          │                          │
   │── send(chat, to=B) ────→│                          │
   │                          │── is_friend(A,B)?       │
   │                          │── save_offline_message  │
   │                          │                          │
   │                          │── B在线?                 │
   │                          │    是: send(chat,B) ──→│
   │                          │    否: send(chat, "离线")│
   │                          │                          │
   │                          │                          │── receipt? ──→│
   │←── status_update ───────│←────────────────────────│
```

### 6.2 群聊消息流程

```
Client A                    Server                     Client B, C
   │                          │                          │
   │── send(group_chat) ────→│                          │
   │                          │── 验证群存在 + A是成员    │
   │                          │                          │
   │                          │── 为B: save(msg_A_B)     │
   │                          │── 为C: save(msg_A_C)     │
   │                          │                          │
   │                          │── notify_group_members ─→│ (B, C 同时)
   │                          │   type=group_chat        │
   │                          │   from=A                 │
```

### 6.3 文件传输流程

```
Sender                      Server                    Receiver
   │                          │                          │
   │── send(file) ──────────→│                          │
   │                          │── save_file_request      │
   │                          │                          │
   │                          │── send(file_request) ──→│
   │                          │                          │
   │                          │                          │── 弹窗: 接受?
   │                          │                          │
   │                          │←── file_response ───────│
   │                          │    (accept/reject)       │
   │                          │                          │
   │                          │   accept:                │
   │                          │── send(file) ──────────→│ (传输文件数据)
   │                          │                          │
   │←── 通知 ────────────────│                          │
```

---

## 7. 安全设计

### 7.1 传输安全

- **SSL/TLS**: 自签名证书，TLS 1.2+，防止中间人窃听
- **证书生成**: `SSL/gen_cert.py`（cryptography 库），有效期 10 年
- **当前限制**: 客户端未启用严格主机名验证

### 7.2 认证安全

- **密码存储**: bcrypt 哈希（自动加盐），不存储明文
- **当前限制**: 密码通过 SSL 加密传输但未在客户端预哈希
- **当前限制**: 无用户名格式验证，无 SQL 注入防护（使用参数化查询已部分缓解）

### 7.3 已知安全风险

| 风险 | 严重程度 | 状态 |
|------|---------|------|
| 密码明文传输（仅 SSL 保护）| 中 | 计划客户端预哈希 |
| 无速率限制 | 中 | 计划添加 |
| 无登录失败锁定 | 低 | 计划添加 |
| 用户名无格式验证 | 低 | 计划添加 |
| 无操作审计日志 | 低 | 计划添加 |

---

## 8. 测试体系

### 8.1 测试分层

```
┌──────────────────────────────────────────────┐
│ L4  端到端测试 (test_e2e.py)     4 tests     │  ← 最慢 (~31s)
│     多客户端 TCP 连接，真实业务场景          │
├──────────────────────────────────────────────┤
│ L3  服务端集成测试 (test_server.py) 16 tests │
│     socketpair mock SSL，完整 message 流     │
├──────────────────────────────────────────────┤
│ L2  客户端逻辑测试 (test_client_logic.py) 19 │
│     纯数据逻辑，不依赖 GUI                   │
├──────────────────────────────────────────────┤
│ L1  数据库层测试 (test_database.py) 35 tests │  ← 投入产出比最高
│     SQLite 临时数据库，全 CRUD 覆盖           │
├──────────────────────────────────────────────┤
│ L0  协议层测试 (test_protocol.py)  15 tests  │  ← 最快 (0.02s)
│     socketpair 编解码验证                     │
└──────────────────────────────────────────────┘
```

### 8.2 运行命令

```bash
# 一键全量
./run_tests.sh

# 开发中快速测试
./run_tests.sh --quick

# 单个文件
.venv/bin/python -m pytest tests/test_database.py -v

# 单个测试
.venv/bin/python -m pytest tests/test_database.py::TestFriendSystem::test_accept_friend_request -v

# 调试模式（显示 log）
.venv/bin/python -m pytest tests/ -v -s --log-cli-level=INFO
```

### 8.3 测试覆盖范围

| 被测模块 | 测试数 | 覆盖的功能点 |
|----------|--------|------------|
| `protocol.py` | 15 | 编解码、分块、空消息、多消息、类型转换、粘包 |
| `database.py` | 35 | 用户CRUD、离线消息全生命周期、好友请求/接受/拒绝、群组CRUD、文件请求、级联删除 |
| 客户端逻辑 | 19 | 状态追踪、文件请求队列、历史管理、消息映射、头解析、去重 |
| `server_client_handler` | 6 | 登录/注册/管理员认证/密码错误/用户不存在/重复注册 |
| `server_message_handler` | 3 | 私聊路由、非好友拦截、离线消息投递 |
| 好友系统 | 2 | 好友请求/接受/拒绝完整流程 |
| 群组系统 | 3 | 创建/加入/群聊路由 |
| 管理员命令 | 2 | 用户列表/公告广播 |
| 多客户端 E2E | 4 | 注册私聊、离线消息、群组创建+群聊、消息撤回 |

---

## 9. 部署与运行

### 9.1 环境要求

- Python 3.12+
- 依赖：`pip install bcrypt pytest cryptography pyopenssl`

### 9.2 首次运行

```bash
# 1. 创建虚拟环境
python -m venv .venv
source .venv/bin/activate

# 2. 安装依赖
pip install bcrypt pytest

# 3. 生成 SSL 证书（如 SSL/ 目录为空或证书过期）
python SSL/gen_cert.py

# 4. 启动服务端
python server/server_main.py

# 5. 启动客户端（新终端窗口）
python client/gui/gui_main.py
```

### 9.3 配置项

当前所有配置硬编码在源码中：

| 配置项 | 位置 | 默认值 |
|--------|------|--------|
| 服务端地址 | `server_main.py` / `gui_login_ui.py` | 127.0.0.1:8090 |
| SSL 证书路径 | `server_main.py` / `gui_login_ui.py` | SSL/tsetcn.crt |
| 消息分块大小 | `protocol.py` | 4MB |
| 撤回时间限制 | `server_message_handler.py` | 2 分钟 |
| 数据库文件名 | `database.py` | users.db |

> 计划迁移到 `config.yaml`。

---

## 10. 已知问题与改进方向

### 10.1 已记录的设计缺陷

| ID | 问题 | 影响 | 状态 |
|----|------|------|------|
| ~~BUG-01~~ | `all_members_responded()` 把发送者也计入需响应成员 | 群文件请求永不自动清理 | ✅ 已修复 (v3.0.1) |
| ~~BUG-02~~ | `add_friend_request()` 未禁止自己加自己 | 好友列表可能异常 | ✅ 已修复 (v3.0.1) |
| ~~BUG-04~~ | `datetime.utcnow()` 已废弃 | Python 3.12+ 警告 | ✅ 已修复 (v3.0.0) |
| ~~BUG-05~~ | SSL 证书仅 1 年有效期 | 到期后连接失败 | ✅ 已修复 (v3.0.0) |

### 10.2 阶段 1：后端加固（高优先级，当前即应执行）

> **原则**: 在更换客户端技术栈之前，先补完后端的功能欠账，避免"边翻译边改规格"。

1. **聊天记录持久化** —— 新建 `message_history` 表永久保存全量消息（chat / group_chat / file），客户端分页拉取，支持向上滚动加载历史
2. **配置文件** —— 用 `config.yaml` 替代所有硬编码（地址、端口、证书路径、分块大小、撤回时限等）
3. **输入验证** —— 用户名格式校验（长度、字符集），防 SQL 注入（当前参数化查询已部分缓解）
4. **协议冻结** —— 阶段 1 结束时明确协议的最终版本，作为后续多端共用的契约

### 10.3 阶段 2–5：客户端技术栈迁移至 Flutter

> **战略决策（2026-06-14）**: 经全面评估，决定采用 **Flutter (Dart)** 作为统一的三端（Windows / Linux / Android）客户端技术栈，逐步替代当前 tkinter 客户端。详见[第 11 章](#11-客户端技术栈演进路线)。

| 阶段 | 内容 | 预估周期 |
|------|------|----------|
| 阶段 2 | Dart 协议层移植：用 Dart 实现 `protocol.py` 完整编解码 + 单元测试 | 1–2 天 |
| 阶段 3 | Flutter 桌面端：登录/注册、好友列表+私聊、群组聊天、文件传输、管理员面板 | 2–4 周 |
| 阶段 4 | Flutter 安卓端：移动端布局适配、通知推送、触控优化 | 1–2 周 |
| 阶段 5 | 打包发布：`flutter build windows` (`.exe`)、`flutter build linux` (AppImage/snap)、`flutter build apk` (`.apk`) | 1 周 |

**可复用资产**: 服务端 (`server/`)、数据库层 (`database.py`)、SSL 证书、全部 89 个测试 **完全不变**。仅重写客户端界面层。

### 10.4 长期展望（阶段 6+）

1. **数据库连接池** —— 减少频繁开/关连接的开销，提升多用户并发性能
2. **端到端加密** —— 客户端预哈希密码 + 消息内容加密
3. **安全审计日志** —— 记录敏感操作（删除用户、管理员命令等）
4. **并发优化** —— 细化锁粒度，减少线程阻塞
5. **Web 端移植** —— Flutter Web 编译，扩大覆盖面
6. **音视频通话** —— WebRTC 集成
7. **消息搜索** —— Elasticsearch 全文索引
8. **机器人平台** —— Webhook 消息推送接口
9. **多语言界面** —— Flutter i18n 支持

---

## 11. 客户端技术栈演进路线

### 11.1 tkinter 现状评估

当前客户端使用 Python tkinter (ttk) 实现。经评估，tkinter 存在以下不可逾越的限制：

| 能做到 | 做不到 |
|--------|--------|
| 统一主题配色（ttk styles） | 圆角聊天气泡（需 Canvas 自绘，极其繁琐） |
| 基本间距和排版优化 | 平滑滚动动画 |
| 自定义字体 | 消息富文本 / 内嵌图片 |
| 基本响应式窗口 | 现代微交互（hover 效果、transition 动画） |
| 第三方主题包（ttkbootstrap） | 深色模式无缝切换 |

**结论**: 对于一个聊天应用（聊天气泡、头像、在线状态指示灯、文件预览），tkinter 的现代化成本极高而效果有限，投入产出比低。

### 11.2 跨平台方案对比

| 方案 | Windows | Linux | Android | 一套代码 | 成熟度 | 适合度 |
|------|---------|-------|---------|----------|--------|--------|
| **Flutter (Dart)** | ✅ | ✅ | ✅ | ✅ 同一套 | ⭐⭐⭐⭐⭐ | 🏆 最佳 |
| Compose Multiplatform | ✅ | ⚠️ 不成熟 | ✅ | ✅ | ⭐⭐⭐ | 待观察 |
| React Native + Electron | ✅ | ✅ | ✅ | ❌ 两套 | ⭐⭐⭐⭐ | 维护重 |
| Tauri + Capacitor | ✅ | ✅ | ✅ | ⚠️ 部分共享 | ⭐⭐⭐ | 复杂度高 |
| Kivy (Python) | ✅ | ✅ | ✅ | ✅ | ⭐⭐ | UI 同样受限 |
| .NET MAUI | ✅ | ❌ | ✅ | ✅ | ⭐⭐⭐ | Linux 缺位 |

### 11.3 选择 Flutter 的理由

1. **真正的单代码库** —— 同一份 Dart 代码编译为 Windows `.exe`、Linux 二进制、Android APK
2. **Material Design 3** 开箱即用 —— 聊天气泡、列表、动画全部内置组件，无需从零造轮子
3. **成熟生态** —— Google 维护，大量即时通讯类 App 案例（腾讯企业微信等均有 Flutter 实践）
4. **状态管理** —— Provider / Riverpod / BLoC 等成熟方案，天然适合聊天应用的实时消息流
5. **网络层直接对接** —— `dart:io` 的 Socket API 可以直接实现现有自定义二进制协议，无需中间层
6. **编译为原生代码** —— 非 WebView，性能接近原生

### 11.4 现有资产可复用性

```
保留不变（零改动）:
├── server/ 全部服务端代码
├── database.py SQLite 数据层
├── protocol.py 协议格式定义（需在 Dart 中重写实现）
├── SSL/ 证书
├── tests/ 全部 89 个测试
└── users.db 数据库文件格式

需要重写:
└── client/gui/*.py 全部 6 个文件 → Flutter 重写
```

### 11.5 迁移路线图

```
阶段 1：后端加固（1–2 周）           ← 🔴 当前阶段
├── 聊天记录持久化（message_history 表）
├── config.yaml 替代硬编码
├── 用户名格式验证
└── 协议最终冻结（作为多端契约）

阶段 2：Dart 协议层（1–2 天）         ← 后端稳定后立即做
├── 在 Dart 中实现 protocol.py 完整编解码
└── 编写 Dart 协议层单元测试

阶段 3：Flutter 桌面端（2–4 周）      ← 先做桌面验证协议
├── 登录 / 注册界面
├── 好友列表 + 私聊窗口
├── 群组聊天
├── 文件传输
└── 管理员面板

阶段 4：Flutter 安卓端（1–2 周）      ← 桌面端完成后
├── 适配移动端布局
├── 通知推送
└── 触控优化

阶段 5：打包发布（1 周）
├── Windows:  flutter build windows → .exe
├── Linux:    flutter build linux   → AppImage / snap
└── Android:  flutter build apk     → .apk
```

### 11.6 核心原则

1. **先稳定后端和协议，再换前端** —— 不在"边开发新功能边换技术栈"的泥潭中挣扎
2. **保留 Python 服务端不变** —— 服务端已稳定且测试完备，没有迁移必要
3. **协议作为唯一契约** —— `protocol.py` 的格式是服务端和所有客户端的唯一接口标准
4. **tkinter 客户端保留至 Flutter 桌面端功能完整** —— 保证开发期间系统始终可用

---

## 变更记录

| 版本 | 日期 | 变更内容 |
|------|------|---------|
| 1.0.0 | 2025-07-02 | 初始版本：SSL 加密、基本聊天、离线消息 |
| 2.0.0 | 2025-07-13 | 好友功能、群组聊天、文件传输、消息回执/撤回 |
| 3.0.0 | 2026-06-14 | 测试体系（89 tests）、import 规范化、`utcnow` 修复、证书续期至 2036 年、本文档更新 |
| 3.0.1 | 2026-06-14 | **BUG-01**: `all_members_responded` 排除发送者（`database.py:337`）; **BUG-02**: `add_friend_request` 禁止自添加（`database.py:356`）。原 BUG-03 经核实为测试辅助函数问题（非生产代码缺陷），已在 3.0.0 中随测试框架一并修正 |
| 3.1.0 | 2026-06-14 | **技术栈决策**: 经全面评估，确定 Flutter (Dart) 为统一三端客户端技术栈；新增第 11 章"客户端技术栈演进路线"；明确 5 阶段迁移路线图；更新短期/中期/长期改进优先级 |
